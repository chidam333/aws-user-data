js_path /etc/nginx/njs/;

js_import benchmark from benchmark.njs;

server {
    listen [::]:80;
    server_name benchmark-orange.chidam.xyz;

    location / {
        js_content benchmark.metrics;
    }
}